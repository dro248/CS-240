package server.database;

import shared.model.*;

public class RecordDAO 
{

	/**
	 * Adds a record to the database.
	 * @param record
	 * @return Whether or not record was successfully added to database.
	 */
	public boolean addRecord(Record record) { return false; }
	
	/**
	 * Get record by recordID
	 * @param recordID
	 * @return Record
	 */
	public Record getRecord(int recordID) { return null; }
}
